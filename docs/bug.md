# BẢNG 1: Rà soát các tính năng có trong giải pháp Loyalty
*(Thực hiện bằng tài khoản Quản trị viên admin)*
- **Tài khoản**: admin
- **Mật khẩu**: Admin@123456

---

| STT | Phân hệ | URL Path | Chức năng kiểm thử | Tiêu chí nghiệm thu (Acceptance Criteria) | Kết quả | Ghi chú | Progress |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C01 | Xác thực | /auth/login | Đăng nhập đúng thông tin | Đăng nhập thành công, lưu JWT Token vào LocalStorage, điều hướng về /dashboard. | PASS | | |
| C02 | Xác thực | /auth/login | Đăng nhập sai mật khẩu | Báo lỗi i18n "Tên đăng nhập hoặc mật khẩu không chính xác", không chuyển trang. | PASS | | |
| C03 | Xác thực | Header | Đăng xuất (Logout) | Hiển thị Dialog xác nhận, xóa Token phiên làm việc và quay về màn hình Login. | PASS | | |
| C04 | Đa ngôn ngữ | Header | Chuyển đổi 7 ngôn ngữ (VI, EN, FR, HT, ZH, JA, KO) | 100% Menu, tiêu đề, nút bấm, placeholder, thông báo dịch chuẩn xác (Không lọt text gốc). | FALSE | Mới chỉ có tiếng việt và tiếng anh còn những ngôn ngữ khác thì chưa có | |
| C05 | Giao diện bảng | Mọi trang | Bộ lọc, Phân trang, Sắp xếp | Lọc từ khóa, lọc theo Tenant (NATCASH/MICRO_CRM), phân trang 10/20/50 dòng hoạt động mượt mà. | PASS | | |
| C06 | Cá nhân | /profile/change-profile | Xem thông tin cá nhân | Hiển thị đúng Username, Email, Số điện thoại và Danh sách Vai trò của tài khoản đang đăng nhập. | PASS | | |
| C07 | Cá nhân | /profile/change-password | Đổi mật khẩu cá nhân | Kiểm tra nhập mật khẩu cũ/mới, xác nhận mật khẩu mới và thông báo thành công. | FALSE | Đang lỗi 500 Internal Server Error | |
| C08 | Tổng quan (DashBoard) | /dashboard | Kiểm tra giao diện và các thông số hiện lên trên màn hình dashboard | Giao diện không bị vỡ các tham số có trong db được hiện lên đầy đủ | FALSE | Phần giao diện menu của quản trị hệ thống đang bị đẩy xuống dưới khiến giao diện không được đẹp mắt, còn lại thì các thông số được thống kê đầy đủ | Done |
| C09 | Chính sách loyalty(Hạng hội viên) | /tiers | Kiểm tra các tính năng sửa các trường có trong tính năng như Tên , ngưỡng điểm, hệ số , lượt quay miễn phí, trạng thái | Các thông tin được cập nhật lên giao diện và các api hoạt động tốt | PASS | | |
| C10 | Chính sách loyalty(Điểm) | /policies | Xem danh sách các chính sách, thêm sửa xoá các chính sách | Hiển thị đầy đủ các chính sách và được phân trang, thêm sửa xoá được các chính sách và được hiển thị cập nhật trên giao diện | FALSE | Không thêm, sửa, xoá được các chính sách, lỗi 500 | |
| C11 | Chính sách loyalty(Cột mốc) | /campaigns | Xem danh sách các chính sách, thêm sửa xoá các chiến dịch và cột mốc | Hiển thị đầy đủ các chiến dịch và được phân trang, Thêm sửa xoá được các chính sách và được hiển thị cập nhật trên giao diện | PASS | | |
| C12 | Khuyến mãi, Game(Voucher) | /vouchers | Xem danh sách các chính sách, thêm sửa xoá các voucher, nhập csv mã voucher | Hiển thị đầy đủ các voucher và được phân trang, Thêm sửa xoá được các chính sách và được hiển thị cập nhật trên giao diện, thêm bằng CSV được | FALSE | Chức năng thêm voucher bằng file csv không khả dụng | |
| C13 | Khuyến mãi, Game(Danh mục trò chơi) | /games | Xem danh sách, tìm kiếm, thêm game mới , sửa các thông tin game, xoá các game | Hiển thị danh sách game, tìm kiếm game theo từ khoá, sửa được các thông tin game,xoá được game khỏi danh sách | FALSE | Không thêm được game mới vào, | |
| C14 | Khuyến mãi, Game(Danh mục trò chơi) | /games | Xem bảng quản lý các trò chơi, tổng lượt chơi, tổng ngân sách đã chi cơ chế khoá redission Rlock | Hiển thị đày đủ các thông tin và tham số trên | PASS | | |
| C15 | Khuyến mãi, Game(Danh mục trò chơi) | /games | Quản lý Cơ cấu giải thưởng của từng game, sử dụng được cơ chế tự động cân bằng thêm được hạng giải thưởng mới , sửa xoá các hạng giải thưởng | Xem danh sách các hạng giải thưởng, sử dụng cơ chế tự động cần bằng sẽ thay đổi tỉ lệ, | FALSE | Không sửa và xoá được các mã hạng giải thưởng | |
| C16 | Khuyến mãi, Game(Cấu hình chung cổng game) | /games/config | Xem bảng quản trị các thông số của cổng game, sửa và lưu cấu hình của cổng game, sửa thông điệp khi vào cổng game, thêm sửa các ô thưởng | Các tính năng hoạt động bình thường không bị lỗi | FALSE | Chức năng cập nhật ô thưởng khôgn sửa được các thông số và không thêm được các ô thưởng nữa lỗi 400 bad requesr | |
| C17 | Đối tác và giao dịch (Quản lsy dối tác tích hợp) | /partners | Xem danh sách thêm sửa xoá thôgn tin của đối tác có trong hệ thống | Danh sách đối tác được hiển thị và phân trang đày đủ các thông tin được thay đổi thì phải được cập nhật ngay trên giao diện | FALSE | Không lấy được danh sách (500), không thêm sửa và xoá được các đối tác | |
| C18 | Đối tác và giao dịch (Số cải điểm , giao dịch) | /transactions | Xem được lịch sử giao dịch, biến động điểm của khách hàng mà đang dùng hệ thống, lọc theo từng đối tác, tìm kiếm theo mã GD SDT Tên đối tác | Danh sách các giao dịch, biến động điểm được hiển thị chuẩn và được phân trang, tính năng lọc và tìm kiếm hoạt động bình thường | FALSE | Xem trong log thì có lấy được danh sách ledger, nhưng transaction thì không thấy gọi đến nên chưa | |
| C19 | Đối tác và giao dịch (Quyết toán bù trừ đa phương) | /clearing | Quản lý tổng giao dịch, tổng điểm tiêu dùng và dư nợ ròng | Hiển thị danh sách các đối tác cần bù trừ khi kết chuyển kỳ quyết toán thì sẽ được xử lý | FALSE | Lỗi 500 nhưng giao diện vẫn hiển thị là thành công | |
| C20 | Quản trị hệ thống(Quản lý tài khoản quản trị) | /admin/users | Lấy ds các tk quản trị, thêm sửa xoá thông tin cấp quyền cho các tài khoản | Hiển thị danh sách các tài khoản và các thông tin sau khi được cập nhật, các tài khoản được thêm xoá thì giao diện cũng phải cập nhật, tìm kiếm được theo tên | FALSE | Không lấy dược danh sách tk admin lỗi 500, khi thêm lúc chọn role thì không có vì api lấy role đang lỗi 500 | |
| C21 | Quản trị hệ thống(Quản lý vai trò) | /admin/roles | Lấy ds các role hiện có, thêm sửa xoá role, tìm kiếm theo tên role | Xem được danh sách các role hiện có, thêm cập nhật thông tin của role được hiển thị ngay trên giao diện sau khi cập nhật/thêm, các role bị xoá thì phải biến mất trên giao diện | FALSE | Không lấy được danh sách role lỗi 500 báo từ backend | |
| C22 | Quản trị hệ thống(Tham số nền tảng loyalty) | /system-parameters | Xem danh sách các tham số, thêm sửa thông tin | Hiển thị danh sách các tham số có trong hệ thống, thêm sửa các thông tin có trong tham số | FALSE | Có xem được danh sách các tham số nhưng không sửa được các trường có trong từng tham số lỗi 500 báo từ server, chức năng thêm cũng báo 500 từ server | |
| C23 | Quản trị hệ thống(Nhật ký hoạt động) | /admin/audit-logs | Xem danh sách các thao tác với các bảng có trong hệ thống | Xem thống kê theo bảng, thao tác, người thực hiện và khoảng ngày tháng | FALSE | Không lấy được danh sách lỗi 500 | |
| C24 | Quản trị hệ thống(Lỗi webhook) | /admin/dead-letter | Quản lý danh sách lỗi webhook | Xem được các thông tin của lỗi webhook, bơm bù toàn bộ hoặc gửi lại từng sự kiện 1 | PASS | | |